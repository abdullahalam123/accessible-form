import { Metadata } from "next";

import SignupPage from "./page";

export const metadata: Metadata = {
  title: "Signup",
  description: "This is the page for users to sign up",
};

export default SignupPage;
