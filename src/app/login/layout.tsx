import { Metadata } from "next";

import LoginPage from "./page";

export const metadata: Metadata = {
  title: "Login",
  description: "This is the page for users to log in",
};
export default LoginPage;
